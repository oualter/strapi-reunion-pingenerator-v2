'use strict';

/**
 * mentions-legales controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::mentions-legales.mentions-legales');
